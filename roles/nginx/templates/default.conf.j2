server {
    listen 80;
    server_name _;
    root /var/www/html;
    index index.html;

    # Serve files directly, 404 if nothing matches
    location / {
        try_files $uri $uri/ =404;
    }
}
